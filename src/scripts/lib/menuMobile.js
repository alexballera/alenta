export default function (){
  var navbarMenu = document.getElementById('navbar')
  const mql = window.matchMedia('screen and (max-width: 680px')
  console.log(mql)
  // if (mql.matches) {
  //   navbarMenu.classList.remove('header__nav--desktop')
  // }else{
  //   navbarMenu.classList.remove('header__nav--mobile')
  // }
}
